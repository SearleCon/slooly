
<img src="/images/clients.png" class="header"> <h2>Client Details</h2><br>


<table>
  <tr>
    <td>Business Name:&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</td>
    <td><h4><%= @client.business_name %></h4></td>
  </tr>
  <tr>
    <td>Contact Person:</td>
    <td><%= @client.contact_person %></td>
  </tr>
  <tr>
    <td>Address:</td>
    <td><%= @client.address %></td>
  </tr>
  <tr>
    <td>City:</td> <!-- &nbsp; SS is the code for Spaces in HTML -->
    <td><%= @client.city %></td>
  </tr>
  <tr>
    <td>Post Code:</td>
    <td><%= @client.post_code %></td>
  </tr>
  <tr>
    <td>Telephone:</td>
    <td><%= @client.telephone %></td>
  </tr>
  <tr>
    <td>Fax:</td>
    <td><%= @client.fax %></td>
  </tr>
  <tr>
    <td>Email:</td>
    <td><%= link_to @client.email, "mailto:"+@client.email %></td>
  </tr>

</table><br>


<%= link_to 'Edit Client', edit_client_path(@client) %> |
<%= link_to 'Back', :back %>

<hr class="bs-docs-separator">











<div class="tabbable tabs-top">
  <ul id="myTab" class="nav nav-tabs">
    <li class="active"><a href="#chasing" data-toggle="tab">Currently Chasing</a></li>
    <li><a href="#history" data-toggle="tab">Chasing History</a></li>
  </ul>

  <div id="myTabContent" class="tab-content">
    <div class="tab-pane fade in active" id="chasing">
		<% if @client_invoices_chasing.count > 0 %>
		  You are currently chasing the following <b><%= @client_invoices_chasing.count.to_s+" invoice(s)" %></b> for <b><%= @client.business_name %></b> to the value of <b><%= Client.total_chasing_outstanding(@client.id) %></b> (total chasing amount). <br>


		<%= render 'invoices_for_client' %>

		<% else %>
		  You are <b>not</b> chasing any invoices for <%= @client.business_name %>.
		<% end %>
		

		
    </div>
    <div class="tab-pane fade in" id="history">
		<% if @client_invoices_history.count > 0 %>
		  Here are the emails you have <b>already sent</b> to <b><%= @client.business_name %></b> (Newest to Oldest).<br>
		
		  <%= render 'history_for_client' %>
		
		<% else %>
		  You have <b>no</b> chase history for <%= @client.business_name %>.
		<% end %>
    </div>
  </div>
</div>






<hr class="bs-docs-separator">







<div><a data-toggle="modal" href="#myModal" >Add invoice for this client</a></div>

<div class="modal hide fade" id="myModal">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">×</button>
    <h3>Add Invoice (for <%= @client.business_name %>)</h3>
  </div>
  <div class="modal-body">
	<%= render 'quick_invoice' %>
  </div>
  <div class="modal-footer">
    <a href="#" class="btn" data-dismiss="modal">Close</a>
  </div>
</div>
