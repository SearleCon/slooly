<p id="notice"><%= notice %></p>
<h2>Client Details</h2><br>

<table>
  <tr>
    <td>Business Name:&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</td>
    <td><h4><%= @client.business_name %></h4></td>
  </tr>
  <tr>
    <td>Contact Person:</td>
    <td><%= @client.contact_person %></td>
  </tr>
  <tr>
    <td>Address:</td>
    <td><%= @client.address %></td>
  </tr>
  <tr>
    <td>City:</td> <!-- &nbsp; SS is the code for Spaces in HTML -->
    <td><%= @client.city %></td>
  </tr>
  <tr>
    <td>Post Code:</td>
    <td><%= @client.post_code %></td>
  </tr>
  <tr>
    <td>Telephone:</td>
    <td><%= @client.telephone %></td>
  </tr>
  <tr>
    <td>Fax:</td>
    <td><%= @client.fax %></td>
  </tr>
  <tr>
    <td>Email:</td>
    <td><%= link_to @client.email, "mailto:"+@client.email %></td>
  </tr>

</table>

<hr class="bs-docs-separator">

<%= link_to 'Edit', edit_client_path(@client) %> |
<%= link_to 'Back', :back %>

<hr class="bs-docs-separator">


<div><a data-toggle="modal" href="#myModal" >Add invoice for this client</a></div>


<div class="modal hide fade" id="myModal">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">×</button>
    <h3>Add Invoice (for <%= @client.business_name %>)</h3>
  </div>
  <div class="modal-body">
	<%= render 'quick_invoice' %>
  </div>
  <div class="modal-footer">
    <a href="#" class="btn" data-dismiss="modal">Close</a>
  </div>
</div>

<hr class="bs-docs-separator">

<% if @client.invoices.count > 0 %>
  You are currently chasing <%= link_to @client.invoices.count.to_s+" invoice(s)", clients_path %> for <%= @client.business_name %> with a total CHASING outstanding amount of <%= Client.total_chasing_outstanding(@client.id) %>.
<% else %>
  You are not currently chasing any invoices for <%= @client.business_name %>.
<% end %>

