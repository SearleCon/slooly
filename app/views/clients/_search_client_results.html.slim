br
.alert.alert-block.alert-info.fade.in
  button.close[type="button" data-dismiss="alert"]
    | ×
  b
    = @clients.total_entries
    |  client(s) found
  |  containing the search string 
  b
    = "'#{@q.conditions.collect(&:value).join(' ')}'"
  |  (In their Business Name or Contact Person fields). 
