.page-header
  h1 News Section
  h6 All the latest product information, offers and general business news
  = link_to new_admins_announcement_path do
    = fa_icon 'newspaper-o', text: 'Add News'
- @announcements.each do |announcement|
  .span11.well
    =   image_tag('news.png', class: 'header').concat(h.content_tag :h3, announcement.headline)
    br
    = simple_format(truncate(announcement.description, length: 600) { h.link_to 'Read full article', announcement })
    br
    = content_tag :div, "Posted by #{announcement.posted_by} #{time_ago_in_words(announcement.created_at)} ago".html_safe, class: 'muted'
    .pull-right
      = link_to edit_admins_announcement_path(announcement) do
        = fa_icon 'edit', text: ' Edit'
      '
      = link_to  admins_announcement_path(announcement), method: :delete, data: { confirm: 'Are you sure?' } do
        = fa_icon 'trash', text: 'Delete'


