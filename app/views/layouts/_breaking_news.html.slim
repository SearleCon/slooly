- if announcements.any?
  .breaking-news
    strong> Breaking News: #{link_to latest_announcement.headline, announcements_path}
    | (This message will disappear #{local_time_ago(latest_announcement.expiry_date)})