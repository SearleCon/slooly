<% flash.each do |type, message| %>
    <% if message.is_a?(String) %>
        <div class="<%= alert_class_for(type) %>">
          <button type="button" class="close" data-dismiss="alert">x</button>
          <%= message %>
        </div>
    <% end %>
<% end %>