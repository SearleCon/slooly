<img src="/images/expired.png" class="header">
<b>Note:</b> Your current subscription is valid until: <b><%= current_user.active_subscription.expiry_date.strftime('%A, %d %B %Y') %></b><br>
(You will be prompted to renew when logging in after this date)