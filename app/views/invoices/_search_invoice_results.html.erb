<br>
<div class="alert alert-block alert-info fade in">
  <button type="button" class="close" data-dismiss="alert">&times;</button>

  <b><%= @invoices.total_entries %> invoice(s) found</b> containing the search string <b><%= "'#{@q.conditions.collect(&:value).join(' ')}'" %></b>
  (In their Invoice Number or Description fields). 
</div>